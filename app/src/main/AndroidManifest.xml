<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.weclass">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.INTERNET">
    </uses-permission>

    <uses-permission
        android:name="android.permission.CHANGE_NETWORK_STATE">
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".PassActivity"></activity>
        <activity android:name=".AppActivity"></activity>
        <activity android:name=".AppListActivity"></activity>
        <activity android:name=".AppDetActivity"></activity>
        <activity android:name=".ClassListActivity"></activity>
        <activity android:name=".ReLiActivity"></activity>
        <activity android:name=".ReqDetActivity"></activity>
        <activity android:name=".RoomDetActivity"></activity>

    </application>

</manifest>